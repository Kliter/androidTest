<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.TestApp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewTest.ListViewTestActivity"></activity>
        <activity android:name=".PrefectureListView.PrefectureActivity"></activity>
        <activity android:name=".RecyclerViewTest.RecyclerViewTestActivity"></activity>
        <activity android:name=".PokeCardList.PokeCardListActivity"></activity>
        <activity android:name=".TextInputTest.TextInputTestActivity"></activity>
    </application>
</manifest>