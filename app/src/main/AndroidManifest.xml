<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.TestApp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">

        <!--
			 The API key for Google Maps-based APIs is defined as a string resource.
			 (See the file "res/values/google_maps_api.xml").
			 Note that the API key is linked to the encryption key used to sign the APK.
			 You need a different API key for each encryption key, including the release key that is used to
			 sign the APK for publishing.
			 You can define the keys for the debug and release targets in src/debug/ and src/release/.
		-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GoogleMapTest.GoogleMapTestActivity"
            android:label="@string/title_activity_google_map_test"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewTest.ListViewTestActivity" />
        <activity android:name=".PrefectureListView.PrefectureActivity" />
        <activity android:name=".RecyclerViewTest.RecyclerViewTestActivity" />
        <activity android:name=".PokeCardList.PokeCardListActivity" />
        <activity android:name=".TextInputTest.TextInputTestActivity" />
        <activity android:name=".VolleyTest.VolleyTestActivity" />
        <activity android:name=".VolleyPractice.VolleyPracticeActivity" />
        <activity android:name=".SQLiteTest.SQLiteTestActivity" />
        <activity android:name=".DialogsTest.DialogsTestActivity" />
        <activity android:name=".CustomDialogTest.CustomDialogTestActivity" />
        <activity android:name=".MaterialSearchViewTest.MaterialSearchViewTestActivity" />
        <activity android:name=".GlideTest.GlideTestActivity" />
        <activity android:name=".SetSoftInputModeTest.SetSoftInputModeTestActivity" />
        <activity android:name=".PermissionCheckTest.PermissionCheckTestActivity" />
        <activity android:name=".FirebaseTest.FirebaseTestActivity" />
        <activity android:name=".FirebaseTest.LoginTestActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>